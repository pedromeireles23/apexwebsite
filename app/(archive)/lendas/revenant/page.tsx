import { createLegendMetadata } from "@/app/lib/legend-metadata";

export const metadata = createLegendMetadata({
  name: "Revenant",
  slug: "revenant",
  description: "Dossiê do Revenant, sua história e seu kit de habilidades em Apex Legends.",
});

export default function RevenantPage() {
  return null;
}
