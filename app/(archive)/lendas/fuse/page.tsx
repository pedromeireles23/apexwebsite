import { createLegendMetadata } from "@/app/lib/legend-metadata";

export const metadata = createLegendMetadata({
  name: "Fuse",
  slug: "fuse",
  description: "Dossiê do Fuse, sua história e seu kit de habilidades em Apex Legends.",
});

export default function FusePage() {
  return null;
}
