import { createLegendMetadata } from "@/app/lib/legend-metadata";

export const metadata = createLegendMetadata({
  name: "Octane",
  slug: "octane",
  description: "Dossiê do Octane, sua história e seu kit de habilidades em Apex Legends.",
});

export default function OctanePage() {
  return null;
}
