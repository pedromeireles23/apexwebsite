import { createLegendMetadata } from "@/app/lib/legend-metadata";

export const metadata = createLegendMetadata({
  name: "Mirage",
  slug: "mirage",
  description: "Dossiê do Mirage, sua história e seu kit de habilidades em Apex Legends.",
});

export default function MiragePage() {
  return null;
}
