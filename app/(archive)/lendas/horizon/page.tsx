import { createLegendMetadata } from "@/app/lib/legend-metadata";

export const metadata = createLegendMetadata({
  name: "Horizon",
  slug: "horizon",
  description: "Dossiê da Horizon, sua história e seu kit de habilidades em Apex Legends.",
});

export default function HorizonPage() {
  return null;
}
