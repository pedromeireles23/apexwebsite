import { createLegendMetadata } from "@/app/lib/legend-metadata";

export const metadata = createLegendMetadata({
  name: "Valkyrie",
  slug: "valkyrie",
  description: "Dossiê da Valkyrie, sua história e seu kit de habilidades em Apex Legends.",
});

export default function ValkyriePage() {
  return null;
}
